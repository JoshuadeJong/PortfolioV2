# /src

## Files + Directory Info
```shell
/components # All reusable components are found here
/providers # Things such as state managers and provider
/views # Pages and subviews which use multiple components (aka non-reusable components)
  App.tsx # The main app view

index.ts # Loading point for the 
index.scss #
setupTests.ts # 
global.d.ts #
```